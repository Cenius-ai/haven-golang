# Haven configuration
# Copy to .env and customise as needed.
# The app boots without any .env file — all values have sensible defaults.

# HTTP server port
PORT=8080

# Directory for data files (holdings.json, alerts.json) and cache
HAVEN_DATA_DIR=.
